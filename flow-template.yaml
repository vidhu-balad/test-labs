description: "CI/CD pipeline for labs application"
visibility: private
template:
  artifacts:
    - name: application
      attestations:
        - name: unit-tests
          required: true
    - name: docker-image
      attestations:
        - name: sbom
          required: true
