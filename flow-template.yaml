version: 1
trial:
  artifacts:
    - name: application
      attestations:
        - name: unit-tests
          required: true
    - name: docker-image
      attestations:
        - name: sbom
          required: true
